# Ignore the `build/` directory in the package root.
/build

# macOS metadata files
.DS_Store

# Other non-essential
cp2s3.sh
/docs

# Lambda build artifacts (published via GitHub Releases / S3, not git)
Lambda/**/*.zip
